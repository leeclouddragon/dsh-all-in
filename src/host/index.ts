import type { Context } from '@deepseek-ai/cordis'

export const name = 'dsh-all-in'

/** The game is entirely browser-local; the host half only activates the bundle. */
export function apply(_ctx: Context): void {}
